# Ghost Hub downloads

This repo holds the app list (`index.json`) and the APK downloads (under **Releases**) for my Ghost Android apps.

The **Ghost Hub** app on my phone reads `index.json` from here, so it can install and update the apps from anywhere.
Every file is published automatically when an app is built.